package com.android.awni.csgoguide;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.view.View;

public class MapsActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_maps);
    }

    public void gotoDust2(View view) {
        // TODO: generate positions and data open
        // from to
        // if position onlick two images are populated, one image for stop location and the other for cross placement
    }
}
